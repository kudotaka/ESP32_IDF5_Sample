set(COMPONENT_SRCDIRS .)
set(COMPONENT_ADD_INCLUDEDIRS .)
set(COMPONENT_REQUIRES nvs_flash esp_wifi esp_timer driver esp_lcd lvgl)

list(APPEND COMPONENT_ADD_INCLUDEDIRS common)

list(APPEND COMPONENT_SRCDIRS i2c_bus)
list(APPEND COMPONENT_ADD_INCLUDEDIRS i2c_bus)

if(CONFIG_SOFTWARE_ESP_WIFI_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS wifi)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS wifi)
endif()

if(CONFIG_SOFTWARE_ESP_LCD_UI_SUPPORT OR CONFIG_SOFTWARE_MODEL_SSD1306_I2C)
    list(APPEND COMPONENT_SRCDIRS i2c_ssd1306)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS i2c_ssd1306)
endif()

if(CONFIG_SOFTWARE_UNIT_ENV2_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS sht3x)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS sht3x)
endif()

if(CONFIG_SOFTWARE_UNIT_SK6812_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS sk6812)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS sk6812)
endif()

if(CONFIG_SOFTWARE_UNIT_4DIGIT_DISPLAY_SUPPORT OR CONFIG_SOFTWARE_UNIT_6DIGIT_DISPLAY_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS tm1637)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS tm1637)
endif()

if(CONFIG_SOFTWARE_UNIT_LED_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS led)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS led)
endif()

if(CONFIG_SOFTWARE_UNIT_RTC_SUPPORT)
    if(CONFIG_SOFTWARE_MODEL_RTC_PCF8563)
        list(APPEND COMPONENT_SRCDIRS pcf8563)
        list(APPEND COMPONENT_ADD_INCLUDEDIRS pcf8563)
    endif()
    if(CONFIG_SOFTWARE_MODEL_RTC_RX8900)
        list(APPEND COMPONENT_SRCDIRS rx8900)
        list(APPEND COMPONENT_ADD_INCLUDEDIRS rx8900)
    endif()
endif()

if(CONFIG_SOFTWARE_UNIT_BUTTON_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS button)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS button)
endif()

if(CONFIG_SOFTWARE_I2C_RTC_RX8900_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS rx8900)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS rx8900)
endif()

if(CONFIG_SOFTWARE_I2C_LCD_ST7032_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS st7032)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS st7032)
endif()

if(CONFIG_SOFTWARE_BUZZER_SUPPORT)
    list(APPEND COMPONENT_SRCDIRS buzzer)
    list(APPEND COMPONENT_ADD_INCLUDEDIRS buzzer)
endif()

register_component()
